{
  "name": "@node-llm/monitor-otel",
  "version": "0.1.0",
  "description": "OpenTelemetry integration for NodeLLM Monitor",
  "main": "dist/index.js",
  "type": "module",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "dev": "tsup src/index.ts --format esm --dts --watch",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@node-llm/monitor": "workspace:*",
    "@opentelemetry/api": "^1.7.0",
    "@opentelemetry/sdk-trace-base": "^1.18.1"
  },
  "devDependencies": {
    "@types/node": "^25.2.0",
    "eslint": "^8.56.0",
    "tsup": "^8.0.1",
    "typescript": "^5.3.3",
    "vitest": "^4.0.18"
  }
}
