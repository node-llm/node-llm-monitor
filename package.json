{
  "name": "node-llm-monitor-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "format": "prettier --write .",
    "dev": "pnpm --filter @node-llm/monitor dev"
  },
  "devDependencies": {
    "prettier": "^3.8.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7"
  },
  "lint-staged": {
    "**/*.{ts,js,mjs,json,md}": [
      "prettier --write"
    ]
  },
  "packageManager": "pnpm@10.26.0",
  "pnpm": {
    "overrides": {
      "undici": ">=6.23.0",
      "lodash": ">=4.17.23",
      "lodash-es": ">=4.17.23",
      "esbuild": ">=0.25.0"
    }
  }
}
